community:
  base:
    file-upload:
      dir: /tmp/cem



---
spring:
  config:
    activate:
      on-profile: suyh


community:
  base:
    token-seconds: 36000
    captcha:
      two-factor-auth-enabled: false
    file-upload:
      dir: D:\tmp\cem



---
spring:
  config:
    activate:
      on-profile: suyh_dev

community:
  base:
    token-seconds: 36000
    captcha:
      two-factor-auth-enabled: false


---
spring:
  config:
    activate:
      on-profile: dev

community:
  base:
    file-upload:
      dir: /tmp/cem


---
spring:
  config:
    activate:
      on-profile: wtdev

community:
  base:
    file-upload:
      dir: /tmp/
